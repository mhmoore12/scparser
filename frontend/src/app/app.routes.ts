import { Routes } from '@angular/router';
import { SchedulePageComponent } from './features/schedule-page/schedule-page.component';

export const routes: Routes = [{ path: '', component: SchedulePageComponent }];
